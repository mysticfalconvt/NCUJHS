extends layout
include mixins/_progressForm
block content
    .inner
        h1=title    
        if students

            form.form(action="/progress" method="POST")
                each student in students
                    +progressForm(student)
                input.button(type="submit" value="Submit →")
        else
            a(href="/progress/block6") Block 1
            a(href="/progress/block7") Block 2
            a(href="/progress/block8") Block 3
            a(href="/progress/block9") Block 4